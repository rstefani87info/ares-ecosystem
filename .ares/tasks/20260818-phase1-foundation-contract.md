# Fase 1 – contratto fondativo

## Goal

Portare nel repository un contratto esplicito e condiviso per:

- convenzioni `README` / `docs` / `tasks`;
- naming CLI dei moduli dev tools;
- ruoli di `appSetup`, `config`, `policies`;
- struttura minima dei progetti generati;
- posizione e ruolo del datasource di manutenzione.

## Stato

- [x] 1. Contratto centrale scritto nel ticket `../../tickets/20260819-phase1-dev-tools-foundation-contract.md`
- [x] 2. `ecosystem/datasources/maintenance` riconosciuto come posizione canonica del datasource condiviso
- [ ] 3. Consolidare naming e shape del modello maintenance (`applications` / `tickets` -> modello target condiviso)
- [ ] 4. Allineare i moduli chiave al contratto nei rispettivi documenti e task
